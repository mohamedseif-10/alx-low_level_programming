#include <stdio.h>

/**
 * main - Entry
 *
 * Description: this function prints all alphabet
 *
 * Return: 0 if (success)
*/

int main(void)
{
	for (size_t i =97;i<123;i++)
		putchar(i);
	return (0);
}
